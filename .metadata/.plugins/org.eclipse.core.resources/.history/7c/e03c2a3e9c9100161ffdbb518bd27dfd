var mysql = require("./mysql");
var ejs = require("ejs");


function getCartPage(req,res)
{
	ejs.renderFile('./views/cart.ejs',function(err, result) {
		if (!err) {
			res.end(result);
		}
		else {
			res.end('An error occurred');
			console.log(err);
		}
	});
}

function getCart(req,res)
{
	var response;
	if(req.session.login)
	{
		query = "select c.quantity,i.name,i.description,u.first_name,i.price from cart c,item i,user u where u.id = i.user_id and c.item_id = i.id and u.id = 1"
	}
	else
	{
		response = {"statusCode":401,"data":null};
		res.send(JSON.stringify(response));
	}
}

exports.getCartPage = getCartPage;
exports.getCart = getCart;